{
  "name": "@asylum-games/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Secure desktop companion for the Asylum Games Host Portal",
  "main": "dist/main/main.js",
  "type": "commonjs",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vite-node tests/obs-controller.test.ts && vite-node tests/automation-engine.test.ts && vite-node tests/studio-layout.test.ts && vite-node tests/broadcast-layout.test.ts && vite-node tests/broadcast-token.test.ts && vite-node tests/active-game.test.ts && vite-node tests/zoom-settings.test.ts",
    "dev": "npm run build && electron .",
    "package:mac": "npm run build && electron-builder --mac --arm64 --x64 --dir"
  },
  "devDependencies": {
    "@types/node": "^22.18.8",
    "electron": "39.8.10",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.3"
  },
  "build": {
    "appId": "com.asylumgames.desktop",
    "productName": "Asylum Games Desktop",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "mac": {
      "category": "public.app-category.entertainment",
      "target": [
        {
          "target": "dir",
          "arch": [
            "arm64",
            "x64"
          ]
        }
      ],
      "identity": null
    }
  },
  "dependencies": {
    "obs-websocket-js": "^5.0.8"
  }
}
