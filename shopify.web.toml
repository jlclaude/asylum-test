name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npm run db:local:generate"
dev = "npm run db:local:migrate:deploy && npm exec react-router dev"
